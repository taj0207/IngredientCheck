// Quick test to see if basic SwiftUI works
// Replace the body of IngredientCheckApp temporarily with this to test

/*
var body: some Scene {
    WindowGroup {
        VStack {
            Text("Hello IngredientCheck!")
                .font(.largeTitle)
                .padding()

            Text("If you see this, the app is running!")
                .foregroundColor(.green)

            Image(systemName: "checkmark.circle.fill")
                .font(.system(size: 100))
                .foregroundColor(.green)
        }
    }
}
*/

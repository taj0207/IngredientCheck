//
//  IngredientCheckApp_Simple.swift
//  Simple version for testing
//

import SwiftUI

// Uncomment this and comment out the original IngredientCheckApp to test

/*
@main
struct IngredientCheckApp: App {
    var body: some Scene {
        WindowGroup {
            SimpleTestView()
        }
    }
}

struct SimpleTestView: View {
    var body: some View {
        NavigationView {
            VStack(spacing: 20) {
                Image(systemName: "leaf.circle.fill")
                    .font(.system(size: 80))
                    .foregroundColor(.green)

                Text("IngredientCheck")
                    .font(.largeTitle)
                    .fontWeight(.bold)

                Text("App is running! ✅")
                    .font(.title3)
                    .foregroundColor(.green)

                Divider()
                    .padding()

                VStack(alignment: .leading, spacing: 12) {
                    Text("Quick Actions:")
                        .font(.headline)

                    Button(action: {}) {
                        HStack {
                            Image(systemName: "camera.fill")
                            Text("Scan Ingredients")
                        }
                        .frame(maxWidth: .infinity)
                        .padding()
                        .background(Color.green)
                        .foregroundColor(.white)
                        .cornerRadius(10)
                    }

                    Button(action: {}) {
                        HStack {
                            Image(systemName: "photo.fill")
                            Text("Choose Photo")
                        }
                        .frame(maxWidth: .infinity)
                        .padding()
                        .background(Color.blue)
                        .foregroundColor(.white)
                        .cornerRadius(10)
                    }
                }
                .padding()
            }
            .navigationTitle("Test View")
        }
    }
}
*/
